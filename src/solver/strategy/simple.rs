use super::Solver;

pub fn solve(solver: &Solver) -> (Vec<(u32, u32)>, Vec<(u32, u32)>) {
    let mut revealed_cells: Vec<(u32, u32)> = Vec::new();
    let mut flagged_cells: Vec<(u32, u32)> = Vec::new();

    // TODO! Implement simple solving logic here

    (revealed_cells, flagged_cells)
}